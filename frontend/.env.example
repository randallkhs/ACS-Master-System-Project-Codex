# Backend base URL for server-side dashboard API calls.
# Use an environment-specific origin in production, for example:
# ACS_DASHBOARD_API_BASE_URL=https://api.applecleaningsystems.net
ACS_DASHBOARD_API_BASE_URL=
